---
all:
  children:
    cloud_vms:
      hosts:
        gcp-autoscale-vm:
          ansible_host: "{{ cloud_vm_ip }}"
          ansible_user: ubuntu
          ansible_ssh_private_key_file: ~/.ssh/gcp_key
          ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
